# Backend API base URL used by Vite.
VITE_API_URL=http://localhost:8080/api/v1

# Keep false until the backend Google OAuth endpoints are ready.
VITE_GOOGLE_AUTH_ENABLED=false

# Codex cowork mode: DEV or PM.
VITE_COWORK_AGENT=PM

# if false page return to login origin
VITE_GOOGLE_AUTH_ENABLED=true